# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'FinalProjectCompiler' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for FinalProjectCompiler
pod 'RestEssentials', '~> 4.0.2'
  target 'FinalProjectCompilerTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'FinalProjectCompilerUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
